def test1() @
    echo "Dans test1"
    x = 10
    echo "Dans test1 again, x = " + x
end

def test2(var1, var2) @
    local = 999
    echo "Dans test2, local : " + local + " var1 : " + var1 + " var2 : " + var2
end

def mul(var1, var2) @
    echo "Dans mul(" + var1 + ", " + var2 + ")"
    return var1 * var2
end

echo "En dehors de la fonction"
call test1()
call test2(1, 3)
call test2(5, "Hello")
call test1()
operation = call mul(3, 5)
echo "Re dehors, operation : " + operation