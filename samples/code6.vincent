def factor(val) @
    if val == 1 @
        return 1
    end

    return val * call factor(val - 1)
end

echo "Debut programme"
/~ Multi line comment on 1 line ~/
echo "factor(3) = " + call factor(3) ~ Comment1 comment2 inline_comment
echo "factor(5) = " + call factor(5) ~ Comment on a second line
/~
    Multi line comment on several lines
    into the code
~/
echo "Fin programme"